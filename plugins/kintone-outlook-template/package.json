{
  "name": "outlook-template",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rimraf dist ship",
    "build": "node -e \"console.log('build skipped')\"",
    "lint": "eslint . || exit 0",
    "pack:init": "mkdirp .keys dist && kintone-plugin-packer . --ppk .keys/outlook-template.ppk --out dist/outlook-template.zip",
    "pack": "mkdirp dist && kintone-plugin-packer . --ppk .keys/outlook-template.ppk --out dist/outlook-template.zip"
 
  },
  "devDependencies": {
   
  },
  "engines": {
    "node": ">=18"
  }
}
