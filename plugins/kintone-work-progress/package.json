{
  "name": "kintone-work-progress",
  "private": true,
  "scripts": {
    "clean": "rimraf dist ship",
    "build": "node -e \"console.log('build skipped')\"",
    "lint": "eslint . || exit 0",
    "pack:init": "mkdirp .keys dist && kintone-plugin-packer . --out dist/work-progress-plugin.zip",
    "pack": "mkdirp .keys dist && kintone-plugin-packer . --ppk .keys/work-progress.ppk --out dist/work-progress-pluginplugin.zip ."
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18"
  }
}